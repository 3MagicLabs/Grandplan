"""Desktop application layer: the review/approve controller and (Windows) GUI.

The controller (`review`) is pure, UI-free, and unit-tested; the PySide6 view binds to it and is
integration-tested on Windows.
"""

from __future__ import annotations
